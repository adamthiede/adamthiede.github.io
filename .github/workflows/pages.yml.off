---
name: deploy items
on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
jobs:
  publish-website:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        shell: bash
        run: |
          echo "action!"


